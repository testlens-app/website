---
import '../styles/global.css';
import { Image } from 'astro:assets';
import logo from '../assets/logo.png';
import SignupModal from '../components/SignupModal.jsx';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" href="/favicon.ico" sizes="any">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicons/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicons/favicon-16x16.png">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>TestLens - Tame your flaky tests</title>
  </head>
  <body class="bg-gradient-to-b from-white via-slate-50 to-slate-100 text-dark font-sans min-h-screen flex flex-col">
    <header class="bg-white shadow">
      <div class="max-w-7xl mx-auto px-4 py-6 flex justify-between items-center">
        <div class="flex items-center space-x-3">
          <Image src={logo} alt="TestLens logo" class="w-8 h-8 md:w-10 md:h-10" />
          <h1 class="text-2xl font-bold">TestLens</h1>
        </div>
      </div>
    </header>

    <main class="max-w-5xl mx-auto px-4 py-16 flex-grow">
      <section class="relative mb-24 bg-gradient-to-r from-blue-400 to-sky-200 text-white rounded-xl overflow-hidden p-12">
        <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-12 items-center">
          <div>
            <h2 class="text-4xl font-extrabold mb-4">Tame flaky tests before they disrupt your productivity</h2>
            <p class="text-lg mb-6">
              TestLens helps you keep your test suite reliable by automatically detecting and isolating flaky tests.
            </p>
            <button data-signup-trigger class="bg-blue-600 text-white px-6 py-3 rounded-lg text-lg hover:bg-blue-700 transition cursor-pointer">
              Get Early Access
            </button>
          </div>
          <div class="flex justify-center">
            <Image src={import('../assets/hero.png')} alt="Hero illustration showing PR UI" class="rounded-lg max-h-64 w-full object-contain" />
          </div>
        </div>
      </section>

      <section class="space-y-20">
        <div class="flex flex-col md:flex-row items-center gap-8">
          <div class="flex-1">
            <h3 class="text-2xl font-semibold mb-3 text-dark">1. Open a pull request</h3>
            <p class="text-muted">TestLens starts monitoring your test runs as soon as you push a PR.</p>
          </div>
          <div class="flex-1 h-48 rounded-lg flex items-center justify-center">
            <Image src={import('../assets/process/01 - open pr.png')} alt="Developer opens PR" class="w-full max-h-48 object-contain rounded-lg" />
          </div>
        </div>

        <div class="flex flex-col md:flex-row items-center gap-8">
          <div class="flex-1">
            <h3 class="text-2xl font-semibold mb-3 text-dark">2. TestLens analyzes and reports flaky tests</h3>
            <p class="text-muted">As tests run, TestLens flags flaky or failing tests directly in your PR as comments — including controls to mute them.</p>
          </div>
          <div class="flex-1 h-48 rounded-lg flex items-center justify-center">
            <Image src={import('../assets/process/02 - testlens report.png')} alt="TestLens analyzing" class="w-full max-h-48 object-contain rounded-lg" />
          </div>
        </div>

        <div class="flex flex-col md:flex-row items-center gap-8">
          <div class="flex-1">
            <h3 class="text-2xl font-semibold mb-3 text-dark">3. Mute flaky tests</h3>
            <p class="text-muted">With a single click, you mute flaky tests and TestLens reruns the pipeline — right from the PR comment.</p>
          </div>
          <div class="flex-1 h-48 rounded-lg flex items-center justify-center">
            <Image src={import('../assets/process/03 - mute tests.png')} alt="Developer mutes tests" class="w-full max-h-48 object-contain rounded-lg" />
          </div>
        </div>

        <div class="flex flex-col md:flex-row items-center gap-8">
          <div class="flex-1">
            <h3 class="text-2xl font-semibold mb-3 text-dark">4. PR is green and ready to merge</h3>
            <p class="text-muted">With all flaky tests muted and tests passing, the PR is ready to merge!</p>
          </div>
          <div class="flex-1 h-48 rounded-lg flex items-center justify-center">
            <Image src={import('../assets/process/04 - pr merged.png')} alt="PR merged" class="w-full max-h-48 object-contain rounded-lg" />
          </div>
        </div>
      </section>

      <section class="mt-24 text-center">
        <h3 class="text-3xl font-bold mb-4 text-dark">Get Early Access</h3>
        <p class="text-muted mb-6">
          Join the beta and be the first to streamline your test suite with TestLens.
        </p>
        <button data-signup-trigger class="bg-blue-600 text-white px-6 py-3 rounded-lg text-lg hover:bg-blue-700 transition cursor-pointer">
          Get Early Access
        </button>
      </section>
    </main>

    <footer class="bg-white border-t">
      <div class="max-w-7xl mx-auto px-4 py-8 text-center text-sm text-muted">
        &copy; 2025 TestLens. All rights reserved.
      </div>
    </footer>

    <div id="sign-up-form" class="fixed inset-0 bg-white/30 backdrop-blur-sm flex items-center justify-center z-50 invisible">
      <div class="bg-white p-6 rounded-lg shadow-lg max-w-sm w-full">
        <h2 class="text-xl font-bold mb-2">Get Early Access</h2>
        <p class="text-gray-600 mb-4 text-sm">
          Leave your email and we'll reach out once we extend the beta to more users.
        </p>
        <form
          name="sign-up-form"
          method="POST"
          netlify-honeypot="bot-field"
          data-netlify="true"
        >
          <p class="hidden">
            <label>
              Don’t fill this out if you’re human: <input name="bot-field" />
            </label>
          </p>
          <label class="block mb-2">
            <span class="text-gray-700">Email</span>
            <input
              type="email"
              name="email"
              required
              class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring focus:border-blue-300"
              placeholder="you@example.com"
            />
          </label>
          <div class="mt-4 flex justify-end gap-2">
            <button
              onclick="toggleSignupForm()"
              type="button"
              class="px-4 py-2 bg-gray-200 text-gray-700 rounded hover:bg-gray-300"
            >
              Cancel
            </button>
            <button
              type="submit"
              class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700"
            >
              Sign Up
            </button>
          </div>
        </form>
      </div>
    </div>

    <script is:inline>
      document.querySelectorAll('[data-signup-trigger]').forEach(button => {
        button.addEventListener('click', toggleSignupForm);
      });

      function toggleSignupForm() {
          document.getElementById('sign-up-form').classList.toggle("invisible");
      }
    </script>
  </body>
</html>
